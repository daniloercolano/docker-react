language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t daniloercolano/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true daniloercolano/docker-react npm run test
